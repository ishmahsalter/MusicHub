<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Transparent accent fills/strokes — resolve per-theme for Midnight vs Aurora vs Gold Rush -->
    <attr name="chipActiveFill" format="color"/>
    <attr name="chipActiveStroke" format="color"/>
    <attr name="accentCardFill" format="color"/>
    <attr name="accentCardStroke" format="color"/>
    <attr name="waveformBarColor" format="color"/>
    <attr name="nowPlayingCardBg" format="color"/>
    <!-- Adaptive text colors — set per-theme so @color/text_muted and @color/section_title resolve correctly in Midnight -->
    <attr name="textColorMuted" format="color"/>
    <attr name="textColorSectionTitle" format="color"/>
    <!-- Input / search bar surface colors — overridden in Midnight to dark navy -->
    <attr name="inputBgColor" format="color"/>
    <attr name="inputStrokeColor" format="color"/>
</resources>
